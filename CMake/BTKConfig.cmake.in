# - Config file for the BTK package
# It defines the following variables
#  BTK_INCLUDE_DIRS - include directories for BTK
#  BTK_LIBRARIES    - libraries to link against

"@PACKAGE_INIT@"

SET_AND_CHECK(BTK_INCLUDE_DIRS "@PACKAGE_INCLUDE_INSTALL_DIR@")

# # Compute paths
# get_filename_component(BTK_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
# set(BTK_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")
#
# # Our library dependencies (contains definitions for IMPORTED targets)
# if(NOT TARGET foo AND NOT BTK_BINARY_DIR)
#   include("${BTK_CMAKE_DIR}/BTKTargets.cmake")
# endif()

include("${CMAKE_CURRENT_LIST_DIR}/BTKTargets.cmake")